# V4.1 exclusion analysis

V4.1 retains the V4 policy-valid corpus after 150 V3 records were removed because they did not satisfy the current candidate/reference oracle policy.

The exclusions are not assumed to be uniformly distributed. See `exclusion_analysis.json` and `exclusion_analysis.csv` for dataset, family, split, source-task, and execution-failure counts.
